#!/bin/bash

# PBS -j oe
# PBS -q phi

cd $SCRATCH

execdir="${HOME}/JCoz/benchmarking/toy"
exe=latedays_xprof_sub.sh
args=""

# Copy executable to scratch
cp ${execdir}/${exe} ${exe}

# Run my executable
time ./${exe} ${args}
